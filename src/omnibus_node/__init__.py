"""omnibus-node: the software a lab runs to be a node in Omnibus.

It turns manuscript sources (LaTeX, DocX, text, Markdown, PDF) and figure
folders into a library that corpus (github.com/caseywdunn/corpus) can build
a corpuscle from, keeps the layers corpus does not hold (figure assets,
notes, dead ends), records who added what and when, and serves the layers
each contributor chose to share over the Model Context Protocol.
"""

__version__ = "0.1.0"
