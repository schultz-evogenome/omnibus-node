# Compatibility fixture

`corpus_bib_parser.py` is `bib/parser.py` from corpus (github.com/caseywdunn/corpus), copied unchanged under its MIT license (LICENSE-corpus). The test suite parses the BibTeX this package writes with it, because `corpus run` reads `references.bib` with exactly this code. Refresh it from upstream when corpus changes its parser.
